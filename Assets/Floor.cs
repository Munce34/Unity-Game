using UnityEngine;
using System.Collections;

public class Floor : MonoBehaviour {

	// Use this for initialization
	void Start () {

		// Floor to stay in one place
		transform.position = new Vector3 (0, -3, 0);
	
	}
	
	// Update is called once per frame
	void Update () {
	
	}
}
